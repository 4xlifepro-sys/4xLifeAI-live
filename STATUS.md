# 4xLifeAI — Status Report

**Last updated:** Session ending (after confidence-70 threshold test)

---

## 1. Bugs Fixed Today

All verified fixed and deployed to Railway:

| # | Bug | Root Cause | Fix |
|---|-----|-----------|-----|
| 1 | All pairs returning `[CH_ACCESS_TOKEN_INVALID]` | cTrader OAuth access token expired/revoked | Generated fresh token via `ctrader-ts auth`, updated Railway env var `CTRADER_ACCESS_TOKEN` |
| 2 | XAUUSD showing price:4175070, GBPAUD off 24x | Price conversion formula used wrong power-of-10 scaling for some asset classes | Applied digits/scale factor from `symbol.digits` correctly for each pair |
| 3 | BTCUSD/ETHUSD returning `[BLOCKED_PAYLOAD_TYPE]` | Burst of 50 req/s hit cTrader rate limit | Staggered pair requests with 20-50ms delay between each |
| 4 | Historical fetch only covered 3.5 days | `toTimestamp` pagination logic stopped after first 1000 bars | Rewrote `fetchHistoricalCandles` to loop: after each batch, set `toTimestamp` to oldest bar's timestamp and keep fetching until 6 months of data is collected |
| 5 | Backtest showed negative duration (-3059 min) and 3.3M signals/week | Trade timing reused the same timestamp repeatedly instead of using real candle timestamps | Fixed the loop to use actual `m5[i].timestamp` for each signal's `entryTime` |

---

## 2. Current Backtest Results

**Environment:** Railway, 27 pairs, M5 candles, 3.5 days of real data (limited by cTrader demo account's historical depth for M5).

| Test | TP/SL Config | Signals | Win Rate | Avg Win | Avg Loss | Net Pips | R:R Ratio |
|------|-------------|---------|----------|---------|----------|----------|-----------|
| Original | 1.0/2.0/3.0x ATR TPs, 1.5x ATR SL | 548 | 50.83% | 5.53 | 6.00 | -1,124 | 0.92:1 |
| Tighter SL | 1.2x ATR SL | 565 | 33.21% | 4.78 | 4.20 | -108 | 1.14:1 |
| Wider TPs | 2.0/3.0/5.0x ATR TPs | 559 | 32.10% | 4.78 | 4.20 | -108 | 1.14:1 |
| + Conf >=70 | Same as above | 559 | 32.10% | — | — | still a loss | 1.14:1 |

**Verdict:** Not profitable under any configuration tested.

---

## 3. Key Open Question — Does Confidence Score Predict Anything?

**Observation:** Raising the confidence threshold from 65 to 70 eliminated only 6 signals (559 vs 565) and did not improve win rate. This suggests the confidence score is not correlated with actual win/loss outcome.

**Hypothesis to verify next session:**
- Run `backtest-confidence-analysis.ts` (already written and pushed) on Railway
- Groups all closed trades by confidence bucket (70-75, 75-80, 80-85, 85-90, 90+)
- Shows win rate for each bucket
- If win rates are flat across buckets, the scoring formula is not predictive and needs rethinking

**If confirmed uncorrelated, next step is to redesign the scoring to weight the factors that actually correlate with winning trades** — not just tune thresholds on broken data.

---

## 4. Recommendation

**DO NOT ENABLE LIVE SIGNALS UNTIL PROFITABLE.**

Specifically:
1. Run the confidence analysis first. If it confirms the score is not predictive, redesign the scoring formula.
2. If the score IS predictive (higher buckets = higher win rate), then a higher threshold (e.g. >=75 or >=80) may yield profitability.
3. Need either win rate >=47% (current R:R) OR R:R >=1.5:1 (current win rate). Neither is met.
4. Once a configuration hits break-even or better over the backtest period, then — and only then — enable live signals.

---

## 5. Files Modified This Session

All pushed and deployed to Railway:

- `server/engine.ts` — SL changed from 1.5x to 1.2x ATR; TPs changed from 1x/2x/3x to 2x/3x/5x ATR; confidence threshold raised from 65 to 70
- `server/live-market-feed.ts` — `requestTimeoutMs: 45000` on `connect()` call
- `server/twelvedata-scanner.ts` — `requestTimeoutMs: 45000` on `connect()` call
- `server/backtest.ts` — full rewrite with proper pagination, real candle timestamps, per-trade simulation
- `server/backtest-confidence-analysis.ts` — NEW. Groups trades by confidence bucket, shows win rate per bucket
- `server/backtest-sanity.ts`, `server/backtest-sanity-m5.ts` — diagnostic scripts, already served their purpose

**Current branch:** `main` (all changes merged and deployed)

---

## 6. Next Session — Immediate First Step

Run the confidence analysis on Railway:

```bash
npx tsx server/backtest-confidence-analysis.ts
```

Interpret the output table. That answer decides the next move:
- If win rates are flat → redesign scoring formula
- If win rates correlate with confidence → try threshold >=75 or >=80

---

*Report generated by Verdent at end of session. Do not delete — this is the handoff document for the next session.*
